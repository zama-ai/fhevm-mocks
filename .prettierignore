# directories
_types
_cjs
_esm
typechain-types

.coverage_artifacts
.coverage_cache
.coverage_contracts
abi
artifacts
build
cache
coverage
dist
fhevmTemp
node_modules
types

# vendored verbatim from @fhevm/host-contracts-cleartext's reference adapter — kept byte-identical to
# upstream so re-syncing is a plain copy. Reformatting it here would create a permanent diff.
packages/hardhat-plugin/src/internal/deploy/ethersEthereumLib.ts

# @fhevm/mock-utils is no longer part of the build: it was dropped from the workspaces when the
# hardhat plugin moved to @fhevm/sdk + @fhevm/host-contracts-cleartext. The sources are kept for
# reference, but nothing installs, builds, lints or formats them.
packages/mock-utils

# files
*.env
*.log
.DS_Store
.pnp.*
coverage.json
package-lock.json
pnpm-lock.yaml
yarn.lock

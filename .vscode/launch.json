{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "hardhat test --parallel --grep BBB",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "cwd": "${workspaceFolder}",
      "runtimeArgs": ["hardhat", "test", "--parallel", "--grep", "BBB"]
    }
  ]
}
